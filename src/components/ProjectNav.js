import React from "react";
import { Link } from "react-router-dom";

export const ProjectNav = ({ projectObj }) => {
// let nextProject = parseInt(projectObj.id + 1)
// let previoustProject = parseInt(projectObj.id - 1)

  return console.log(projectObj)
  // (
  //   <nav className="project__nav">
  //     <div className="nav-left__arrow"> <Link to={nextProject}>&#60;</Link> </div>
  //     <p className="project-name__page-title">Page Title</p>
  //     <div className="nav-right__arrow"> <Link to={previoustProject}>&#62;</Link> </div>
  //   </nav>
  // )
};
